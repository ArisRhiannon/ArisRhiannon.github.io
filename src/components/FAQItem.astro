---
interface Props {
  question: string;
  answer: string;
  id: string;
}

const { question, answer, id } = Astro.props;
---

<div class="border border-slate-900 bg-slate-950/10 rounded-md overflow-hidden transition-all duration-300 hover:border-amber-500/20">
  <button
    class="w-full text-left p-4 font-serif text-sm sm:text-base text-gray-200 hover:text-amber-400 flex justify-between items-center transition-colors focus:outline-none"
    aria-expanded="false"
    aria-controls={`faq-answer-${id}`}
    id={`faq-btn-${id}`}
    data-faq-btn
  >
    <span class="tracking-wide">{question}</span>
    <svg
      class="w-4 h-4 text-amber-500 transform transition-transform duration-300"
      fill="none"
      viewBox="0 0 24 24"
      stroke="currentColor"
    >
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
    </svg>
  </button>
  <div
    id={`faq-answer-${id}`}
    class="max-h-0 overflow-hidden transition-all duration-300 ease-in-out"
    aria-labelledby={`faq-btn-${id}`}
    role="region"
  >
    <div class="p-4 pt-0 text-sm text-gray-400 border-t border-slate-950 leading-relaxed font-serif">
      {answer}
    </div>
  </div>
</div>

<script>
  const buttons = document.querySelectorAll('[data-faq-btn]');
  buttons.forEach(button => {
    button.addEventListener('click', () => {
      const expanded = button.getAttribute('aria-expanded') === 'true';
      button.setAttribute('aria-expanded', (!expanded).toString());
      
      const svg = button.querySelector('svg');
      if (svg) {
        if (!expanded) {
          svg.classList.add('rotate-180');
        } else {
          svg.classList.remove('rotate-180');
        }
      }
      
      const answerId = button.getAttribute('aria-controls');
      if (answerId) {
        const answer = document.getElementById(answerId);
        if (answer) {
          if (!expanded) {
            answer.style.maxHeight = answer.scrollHeight + 'px';
          } else {
            answer.style.maxHeight = '0px';
          }
        }
      }
    });
  });
</script>
